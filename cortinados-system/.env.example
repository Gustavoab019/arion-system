# =============================================================================
# SISTEMA DE GESTÃO DE CORTINADOS - VARIÁVEIS DE AMBIENTE
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE
# -----------------------------------------------------------------------------
# MongoDB Atlas connection string
# Formato: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=mongodb+srv://gustavoamorim0180:dPVA3Jrbtc9Q01TH@cluster0.ykwi4dy.mongodb.net/ArionDB?retryWrites=true&w=majority


# -----------------------------------------------------------------------------
# AUTHENTICATION (NextAuth.js)
# -----------------------------------------------------------------------------
# Secret key para NextAuth.js - MUDE EM PRODUÇÃO!
# Gere uma chave segura em: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-secret-key-here

# URL da aplicação
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# JWT TOKENS
# -----------------------------------------------------------------------------
# Chave secreta adicional para tokens customizados
# Use: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# -----------------------------------------------------------------------------
# CLOUD STORAGE (Cloudinary)
# -----------------------------------------------------------------------------
# URL para upload de imagens (fotos de medições, instalações, etc)
# Formato: cloudinary://api_key:api_secret@cloud_name
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Configurações individuais do Cloudinary (alternativa)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# -----------------------------------------------------------------------------
# EMAIL (Opcional - para notificações)
# -----------------------------------------------------------------------------
# SMTP para envio de emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=sistema@empresa.com
SMTP_PASS=your_app_password

# -----------------------------------------------------------------------------
# ENVIRONMENT
# -----------------------------------------------------------------------------
# Ambiente da aplicação
NODE_ENV=development

# -----------------------------------------------------------------------------
# BACKUP & LOGS (Opcional)
# -----------------------------------------------------------------------------
# AWS S3 para backups (opcional)
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1
AWS_BUCKET_NAME=cortinados-backups

# -----------------------------------------------------------------------------
# INSTRUÇÕES DE CONFIGURAÇÃO
# -----------------------------------------------------------------------------
# 1. Copie este arquivo para .env.local
# 2. Substitua todos os valores com suas credenciais reais
# 3. NUNCA commite o arquivo .env.local no Git
# 4. Para produção, configure as variáveis no painel da Vercel
# 5. MongoDB Atlas: https://cloud.mongodb.com
# 6. Cloudinary: https://cloudinary.com
#
# SEGURANÇA:
# - Use senhas fortes (mínimo 32 caracteres)
# - Rotacione as chaves periodicamente
# - Mantenha backups seguros das configurações